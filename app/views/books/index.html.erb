<% @books.each do |book| %>
<div>
  <%= link_to book_path(book.id) do %>
    <%= image_tag book.get_image %>
  <% end %>
  <p>:<%= image_tag 'sample-authori1.jpg' %></p>
  <p>:<%= book.title %></p>
  <p>:<%= book.opinion %></p>
  <p>:<%= book.user.name %></p>
</div>
<% end %>